import styled from '@emotion/styled';

export const Block = styled.section({
  marginLeft: 'auto',
  marginRight: 'auto',
  marginTop: '32px',
  width: '320px',
});

export const Title = styled.h2({
  marginBottom: '16px',
});
